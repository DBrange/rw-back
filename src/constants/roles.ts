export enum ROLES {
    ADMIN = 'ADMIN',
    BROKER = 'BROKER',
    CLIENT = 'CLIENT',
  }
 
export enum ACCESS_LEVEL {
  FREE = 0,
  BASIC = 10,
  PREMIUM = 20,
  GOLD = 30,
}  

export enum AUTHORIZATION{
  AUTHORIZED = 'AUTHORIZED',
  UNAUTHORIZED = 'UNAUTHORIZED'
}
